//Write a function to remove the first element from an array.
function Shift() {
    let arr = [1, 2, 3, 4];
    console.log("Array:",arr);
    arr.shift();
    console.log("After Shift:",arr);
  }
  
Shift();
  // in this diffrence between pop and shift is pop remove the last element of the array & shift remove first element of array.
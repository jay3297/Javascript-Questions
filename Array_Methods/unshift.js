//Write a function to add a new element at the beginning of an array.
function Unshift() {
    let arr = [1, 2, 3];
    console.log("Array:",arr);
    arr.unshift(0);
    console.log("After Unshift:", arr);
  }
  
 Unshift();
// in this differnce between push and unshift is push add the element in a last of array and using unshift this can add beginning of array.
